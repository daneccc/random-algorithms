#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    float n = 4, k = 5;
    printf("%f elevado a %f é igual %f\n\n", n, k, pow(4, 5));

    printf("Raiz quadrada de 60 é: %f\n\n", sqrt(3600));
}